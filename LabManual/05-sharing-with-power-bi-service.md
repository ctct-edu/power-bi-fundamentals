# 演習05 Power BI Serviceで共有

## 演習概要

Power BI Desktopで作成した3ページのPBIXを研修用ワークスペースへ発行します。ブラウザーのPower BI Serviceで同期スライサー、Azure Maps、ドリルスルーを含むレポートの動作を確認し、セマンティックモデル、ワークスペースロール、ダッシュボード、アプリの役割も確認します。

## 到達目標

- Desktopからワークスペースへ発行できる
- Service上で3ページ、同期スライサー、Azure Maps、ドリルスルーを確認できる
- レポートとセマンティックモデルを見分けられる
- ワークスペースロールを確認できる
- ダッシュボードとアプリの用途を説明できる
- 権限の有無に応じて安全に操作を分岐できる

## 所要時間

25分

## 使用ファイル

- `PowerBI演習04_完成レポート.pbix`

## 演習の流れ

| 段階 | 操作する場所 | 実施内容 |
|---|---|---|
| 1 | Power BI Desktop | 保存、サインイン、ワークスペースへ発行 |
| 2 | WebブラウザーのPower BI Service | レポート、セマンティックモデル、ロールを確認 |
| 3 | Power BI Service（権限がある場合） | ダッシュボードへピン留め、アプリを作成 |

> **実務上の注意**
>
> 実務では、発行後にデータソース資格情報、ゲートウェイ接続、更新設定を確認します。
> ワークスペースのロールはグループ単位で必要最小限に割り当て、閲覧者が多い場合はアプリによる配布も検討します。
> Azure MapsはPPUライセンスがあっても、テナント設定やネットワークポリシーによって利用できない場合があります。

## Power BI Desktopから発行する

### 1. 保存とサインインを確認する

1. Power BI Desktopで`PowerBI演習04_完成レポート.pbix`を開きます。
2. `売上サマリー`、`店舗・商品分析`、`地域マップ`の3ページがあることを確認します。
3. `売上サマリー`にKPI、`地域マップ`にAzure Mapsがあることを確認します。
4. 年月と地域のスライサーが3ページで同期し、地図から`店舗・商品分析`へドリルスルーできることを確認します。
5. 各ページに移動し、スライサーとフィルターの選択が解除されていることを確認してからCtrl+Sで保存します。
6. 画面右上にアカウントが表示されていることを確認します。
7. ［サインイン］と表示されている場合は、研修用アカウントでサインインします。

### 2. ワークスペースへ発行する

1. ［ホーム］→［発行］を選択します。
3. 発行先一覧から、研修用アカウントと同じ名前のワークスペースを選択します。
4. ［マイ ワークスペース］が選択されていないことを確認します。
5. ［選択］を押し、完了メッセージが表示されるまでPower BI Desktopを閉じずに待ちます。
7. レポート名または［Power BIで開く］を選択します。

ここから先はWebブラウザーで操作します。アドレス欄に`app.powerbi.com`を含むURLが表示されることを確認してください。

サインインが要求された場合は、ブラウザ上でも研修用アカウントでサインインします。

## Power BI Serviceで発行結果を確認する

### 3. レポートを確認する

1. ブラウザーでレポート名が`PowerBI演習04_完成レポート`になっていることを確認します。
2. `売上サマリー`、`店舗・商品分析`、`地域マップ`の3ページがあることを確認します。
3. `売上サマリー`で年月と地域を1つずつ選択します。
4. `店舗・商品分析`へ移動し、同じ年月と地域が選択され、マトリクス、グラフ、表が絞り込まれていることを確認します。
5. `地域マップ`へ移動し、同じ選択条件でAzure Maps、都道府県別棒、店舗別表が絞り込まれていることを確認します。
6. 地図のマーカーへマウスポインターを合わせ、売上、粗利益、目標達成率のツールヒントを確認します。
7. 任意の都道府県マーカーまたは都道府県別の棒を右クリックし、［ドリルスルー］→［店舗・商品分析］を選択します。
8. 選択した都道府県の店舗だけが表示されたことを確認し、戻るボタンで`地域マップ`へ戻ります。
9. いずれかのページで年月と地域の選択を解除し、ほかのページでも解除されたことを確認します。
10. Desktopと同じ条件で同じ集計値になることを確認します。
11. ［既定値にリセット］または［リセット］を選択し、未選択の状態へ戻します。

### 4. ワークスペースの項目を見分ける

1. 左側のナビゲーションで、先ほどの発行時に選択したワークスペース名を選択します。
2. コンテンツ一覧の［型］列を確認します。

| 型 | 内容 | 確認すること                                          |
|---|---|---|
| レポート | ビジュアルとページ | 3ページ、同期スライサー、地図、ドリルスルーが動作する |
| セマンティック モデル | テーブル、関係、DAX、更新元 | 資格情報、接続、更新の設定領域がある |

3. ［セマンティック モデル］の行にある［…］→［設定］を選択します。
4. ［データ ソースの資格情報］、［ゲートウェイとクラウド接続］、［最新の情報に更新］または同等の領域があることを確認します。
5. 設定値は変更せず、ワークスペースへ戻ります。

ローカルExcelやオンプレミスSQL Serverの更新情報を取得するには、オンプレミスデータゲートウェイの設定が必要です。本演習では設定しません。

### 5. 自分のワークスペースロールを確認する

1. ワークスペースの画面に戻り、［アクセスの管理］を選択します。
2. 自分のアカウントを探し、表示されているロールを確認します。

| ロール | 主な用途 |
|---|---|
| 管理者 | ワークスペース設定とアクセスを含む管理 |
| メンバー | コンテンツ管理とアプリ発行への関与 |
| 共同作成者 | コンテンツの作成・編集・発行 |
| 閲覧者 | 読み取り中心 |

3. 確認後は画面を閉じ、ロールは変更しません。

組織設定やライセンスにより実行できる操作は異なります。必要最小限のロールを使用します。

## 共有コンテンツを作る

### 6. 最初のビジュアルをダッシュボードへピン留めする

1. 型が［レポート］の`PowerBI演習04_完成レポート`を開きます。
2. `売上サマリー`ページで`売上合計`カードへマウスポインターを合わせます。
3. ピンのアイコンを選択します。
4. ［新しいダッシュボード］を選択します。
5. 名前を`みらいリテール研修ダッシュボード`として［ピン留め］を選択します。
6. 完了メッセージを確認してレポートへ戻ります。

### 7. 残りのビジュアルを同じダッシュボードへ追加する

最初のカードと同じ操作で、［既存のダッシュボード］を選び、次の2つを追加します。

| 対象ビジュアル | ピン留め先 |
|---|---|
| 売上達成状況KPI | `みらいリテール研修ダッシュボード` |
| 月別 売上と販売目標 | `みらいリテール研修ダッシュボード` |

1. ワークスペースから作成したダッシュボードを開きます。
2. 3つのタイルをドラッグし、重ならないよう配置と大きさを整えます。
3. 任意のタイルを選択し、元レポートへ移動することを確認します。

### 8. アプリを作成する

1. ワークスペースで［アプリの作成］または［アプリの更新］を選択します。
2. 次の設定表に従って入力します。

| 設定区分 | 設定内容 |
|---|---|
| セットアップ | アプリ名: `みらいリテール 売上分析` / 説明: `研修用の売上分析レポート` |
| コンテンツ | レポートとダッシュボード |
| 対象ユーザー | デフォルト設定のまま                                         |

3. 対象ユーザーが組織全体になっていないことを確認します。
5. ［アプリの発行］を選択します。
6. 完了メッセージまたはアプリへのリンクを確認します。

## 共有方法の使い分け

| 方法 | 主な対象 | 特徴 |
|---|---|---|
| ワークスペース | 作成・運用チーム | ロールを使って共同作業する |
| レポートの共有 | 特定の利用者 | 個別コンテンツへのアクセスを与える |
| ダッシュボード | 閲覧者 | 重要なタイルを1ページに集約する |
| アプリ | 部門・組織の利用者 | 完成した複数コンテンツをまとめて配布する |

「Webに公開」はインターネット上へ匿名公開される可能性があるため、この研修では使用しません。

## 完了条件

- 指定ワークスペースにレポートとセマンティックモデルを発行した
- ブラウザーで3ページ、同期スライサー、Azure Maps、ドリルスルーを確認した
- 自分のワークスペースロールを確認した
- 権限がある場合はダッシュボードとアプリを確認し、ない場合は講師デモを確認した
- ワークスペース、レポート、セマンティックモデル、ダッシュボード、アプリの用途を説明できる

## 追加課題

アプリの対象ユーザーを「経営層」と「店舗責任者」に分ける場合、どのページを見せるか、どの権限が必要かを検討します。

## 研修完了

これで、Excelから始まり、Power Query、複数ソースのモデル、DAX、可視化、Power BI Serviceでの共有までの流れを一通り実践しました。

[演習一覧へ戻る](../README.md)
